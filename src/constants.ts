export enum DatasetType {
    EXISITING = 'EXISITING',
    CREATED_NEW = 'CREATED_NEW',
}

export enum BookingStatus {
    PAYMENT = 'PAYMENT',
    CHECKOUT = 'CHECKOUT',
    PAID = 'PAID',
    EXPIRED = 'EXPIRED',
}
